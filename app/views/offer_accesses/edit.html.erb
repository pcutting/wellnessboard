<h1>Editing offer_access</h1>

<% form_for(@offer_access) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :promotion %><br />
    <%= f.text_field :promotion %>
  </p>
  <p>
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @offer_access %> |
<%= link_to 'Back', offer_accesses_path %>